var _    = require('lodash')
  , chai = require("chai")
chai.should()

var Z = require('../scripts/zoom_manager.js')

describe('ZoomManager', function () {

  describe('zoom_to_summary_level', function () {
    
    it('accepts a number')
    // it('accepts a map')

    it('throws an error without a parameter')
    it('throws an error without a number')// or leaflet map')
    
    it('returns a string')

  })

  describe('summary_level_to_zoom', function () {

    it('accepts a summary level object')
    it('accepts a summary level string')
    it('accepts a summary level suffix')
  })
})